'use client';

import { createAppTheme } from "./base-theme";

// all themes goes here
export const defaultTheme = createAppTheme({
    header: {
        color: "#8A8C99",
    },
    footer: {
        textColor: "#A1A4B2",
    },
});